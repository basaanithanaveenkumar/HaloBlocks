{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"HaloBlocks <p> Modern, Modular, and Composability-First Neural Network Components. </p> <p> </p> <p>HaloBlocks is a high-performance Python library designed for building complex neural network architectures through simple, composable blocks. Whether you are building Transformers, Mixture-of-Experts (MoE), or Vision-Language-Action (VLA) models, HaloBlocks provides the foundational \"bricks\" you need.</p>"},{"location":"#key-features","title":"\u2728 Key Features","text":"<ul> <li>\ud83e\udde9 First-Class Composability: Every component is treated as a \"block\" that can be easily nested and combined.</li> <li>\ud83d\ude80 MoE Ready: Built-in support for advanced Mixture-of-Experts architectures, including routed and shared expert systems.</li> <li>\ud83d\udc41\ufe0f VLA Integration: Optimized blocks for Vision-Language-Action models, featuring specialized decoders and attention mechanisms.</li> <li>\ud83d\udee0\ufe0f Config-Driven Architecture: Build entire models from JSON/YAML configurations using the <code>BlockFactory</code>.</li> <li>\u26a1 Performance Optimized: Native PyTorch implementation with a focus on speed and memory efficiency.</li> </ul>"},{"location":"#installation","title":"\ud83d\ude80 Installation","text":"<p>Install the library via <code>pip</code>:</p> <pre><code>pip install haloblocks\n</code></pre> <p>Or using <code>uv</code> for faster dependency management:</p> <pre><code>uv add haloblocks\n</code></pre>"},{"location":"#quick-start","title":"\ud83d\udee0\ufe0f Quick Start","text":"<p>[!TIP] New to HaloBlocks? Check out our interactive Tutorial Notebook to see the library in action!</p>"},{"location":"#1-keras-like-direct-style-new","title":"1. Keras-like \"Direct\" Style (New! \u2728)","text":"<p>No more nested dictionaries. Access blocks directly from <code>haloblocks.layers</code>:</p> <pre><code>import haloblocks.layers as layers\n\n# Create a block as a class instance\nattn = layers.multi_head_attn(emb_dim=512, num_heads=8)\n</code></pre>"},{"location":"#2-the-convenience-create-function","title":"2. The Convenience <code>create</code> Function","text":"<p>Use the top-level <code>create</code> function for easy instantiation by string name:</p> <pre><code>import haloblocks as hb\n\n# Quick creation with keyword arguments\nattn = hb.create('multi_head_attn', emb_dim=512, num_heads=8)\n</code></pre>"},{"location":"#3-config-driven-style","title":"3. Config-Driven Style","text":"<p>Still fully supported and perfect for YAML/JSON configurations:</p> <pre><code>config = {\n    'type': 'multi_head_attn',\n    'emb_dim': 512,\n    'num_heads': 8\n}\nattn = hb.create(config)\n</code></pre>"},{"location":"#project-structure","title":"\ud83d\udcc2 Project Structure","text":"<pre><code>haloblocks/\n\u251c\u2500\u2500 core/               # Foundational Block, Factory, and Registry\n\u251c\u2500\u2500 blocks/             # Specialized component implementations\n\u2502   \u251c\u2500\u2500 attention/      # Self-Attention, Multi-Head, Scaled Dot-Product\n\u2502   \u251c\u2500\u2500 moe/            # Mixture-of-Experts (DeepSeek style)\n\u2502   \u251c\u2500\u2500 vla/            # Vision-Language-Action specific blocks\n\u2502   \u2514\u2500\u2500 transformer/    # Transformer layers and blocks\n\u2514\u2500\u2500 heads/              # Model output heads (Classification, LM, etc.)\n</code></pre>"},{"location":"#contributing","title":"\ud83e\udd1d Contributing","text":"<p>We welcome contributions! Please see our Contributing Guide to get started.</p>"},{"location":"#license","title":"\ud83d\udcc4 License","text":"<p>HaloBlocks is released under the MIT License.</p> <p>   Built with \u2764\ufe0f by Naveen </p>"},{"location":"tutorial/","title":"\ud83d\udc7c HaloBlocks Tutorial","text":"In\u00a0[\u00a0]: Copied! <pre># Install if needed\n# !pip install haloblocks torch\n\nimport torch\nimport haloblocks as hb\nimport haloblocks.layers as layers\n\nprint(f\"HaloBlocks version: {hb.__version__ if hasattr(hb, '__version__') else '0.1.0'}\")\n</pre> # Install if needed # !pip install haloblocks torch  import torch import haloblocks as hb import haloblocks.layers as layers  print(f\"HaloBlocks version: {hb.__version__ if hasattr(hb, '__version__') else '0.1.0'}\") In\u00a0[\u00a0]: Copied! <pre># Create a Multi-Head Attention layer\nmha = layers.multi_head_attn(emb_dim=256, num_heads=8, causal_mask=True)\n\nprint(f\"Created layer: {mha.name}\")\nprint(f\"Configuration: {mha}\")\n</pre> # Create a Multi-Head Attention layer mha = layers.multi_head_attn(emb_dim=256, num_heads=8, causal_mask=True)  print(f\"Created layer: {mha.name}\") print(f\"Configuration: {mha}\") In\u00a0[\u00a0]: Copied! <pre># Run a dummy forward pass\nx = torch.randn(2, 64, 256)\noutput = mha(x)\nprint(f\"Input shape: {x.shape}\")\nprint(f\"Output shape: {output.shape}\")\n</pre> # Run a dummy forward pass x = torch.randn(2, 64, 256) output = mha(x) print(f\"Input shape: {x.shape}\") print(f\"Output shape: {output.shape}\") In\u00a0[\u00a0]: Copied! <pre># Create an MLP with GELU and no bias on the last layer\nmlp = layers.mlp(\n    input_dim=128, \n    hidden_dims=[256, 256], \n    output_dim=64,\n    activation='gelu',\n    bias=True,\n    last_layer_activation=False  # Final layer is a linear projection\n)\n\nx = torch.randn(4, 128)\nout = mlp(x)\nprint(f\"MLP output shape: {out.shape}\")\nprint(f\"MLP structure: {mlp}\")\n</pre> # Create an MLP with GELU and no bias on the last layer mlp = layers.mlp(     input_dim=128,      hidden_dims=[256, 256],      output_dim=64,     activation='gelu',     bias=True,     last_layer_activation=False  # Final layer is a linear projection )  x = torch.randn(4, 128) out = mlp(x) print(f\"MLP output shape: {out.shape}\") print(f\"MLP structure: {mlp}\") In\u00a0[\u00a0]: Copied! <pre># Create a Scaled Dot Product Attention block\nattn = hb.create('scaled_dot_product_attention', dropout=0.1)\n\n# For SDPA, we need Q, K, V\nq = torch.randn(2, 8, 64, 32)\nk = torch.randn(2, 8, 64, 32)\nv = torch.randn(2, 8, 64, 32)\n\nout = attn(q, k, v)\nprint(f\"SDPA output shape: {out.shape}\")\n</pre> # Create a Scaled Dot Product Attention block attn = hb.create('scaled_dot_product_attention', dropout=0.1)  # For SDPA, we need Q, K, V q = torch.randn(2, 8, 64, 32) k = torch.randn(2, 8, 64, 32) v = torch.randn(2, 8, 64, 32)  out = attn(q, k, v) print(f\"SDPA output shape: {out.shape}\") In\u00a0[\u00a0]: Copied! <pre>config = {\n    'type': 'self_attention_basic',\n    'emb_dim': 128,\n    'return_attn_weights': True\n}\n\nblock = hb.create(config)\nx = torch.randn(1, 10, 128)\nout, weights = block(x)\n\nprint(f\"Attention Weights shape: {weights.shape}\")\n</pre> config = {     'type': 'self_attention_basic',     'emb_dim': 128,     'return_attn_weights': True }  block = hb.create(config) x = torch.randn(1, 10, 128) out, weights = block(x)  print(f\"Attention Weights shape: {weights.shape}\") In\u00a0[\u00a0]: Copied! <pre>composite_config = {\n    'type': 'composite_block',\n    'blocks': [\n        {'type': 'multi_head_attn', 'emb_dim': 128, 'num_heads': 4},\n        {'type': 'mlp', 'input_dim': 128, 'hidden_dims': [256, 128]}\n    ]\n}\n\npipeline = hb.create(composite_config)\nprint(pipeline)\n</pre> composite_config = {     'type': 'composite_block',     'blocks': [         {'type': 'multi_head_attn', 'emb_dim': 128, 'num_heads': 4},         {'type': 'mlp', 'input_dim': 128, 'hidden_dims': [256, 128]}     ] }  pipeline = hb.create(composite_config) print(pipeline)"},{"location":"tutorial/#haloblocks-tutorial","title":"\ud83d\udc7c HaloBlocks Tutorial\u00b6","text":"<p>Welcome to the official tutorial for HaloBlocks!</p> <p>HaloBlocks is a modern, high-performance Python library for modular neural network components. In this notebook, we'll explore how to build and compose models using our three primary interfaces:</p> <ol> <li>Layers API: Direct class instantiation.</li> <li>Factory API: Configuration-driven creation.</li> <li>Composite Blocks: Building sequential pipelines.</li> </ol>"},{"location":"tutorial/#setup","title":"\u2699\ufe0f Setup\u00b6","text":"<p>First, let's ensure we have everything installed and our environment set up.</p>"},{"location":"tutorial/#part-1-using-haloblockslayers-the-keras-style","title":"\ud83e\udde9 Part 1: Using <code>haloblocks.layers</code> (The Keras Style)\u00b6","text":"<p>This is the most direct way to use HaloBlocks. You can access any registered block as if it were a class in the <code>layers</code> module.</p>"},{"location":"tutorial/#part-2-the-configurable-mlp","title":"\ud83e\uddf1 Part 2: The Configurable MLP\u00b6","text":"<p>The <code>MLP</code> block is highly versatile, supporting custom activations, bias control, and flexible output behavior.</p>"},{"location":"tutorial/#part-3-using-the-create-function","title":"\ud83d\udee0\ufe0f Part 3: Using the <code>create</code> Function\u00b6","text":"<p>The <code>hb.create()</code> function is a powerful tool that allows you to instantiate blocks using string names and keyword arguments. This is ideal for quickly swapping components.</p>"},{"location":"tutorial/#part-4-configuration-driven-design","title":"\ud83c\udfd7\ufe0f Part 4: Configuration-Driven Design\u00b6","text":"<p>The <code>create</code> function also accepts dictionaries, making it perfect for loading models from JSON or YAML files.</p>"},{"location":"tutorial/#part-5-composite-blocks","title":"\ud83e\uddf1 Part 5: Composite Blocks\u00b6","text":"<p>HaloBlocks makes it easy to stack blocks together. You can even define these stacks in your configuration!</p>"},{"location":"tutorial/#next-steps","title":"\ud83d\ude80 Next Steps\u00b6","text":"<p>Now that you've mastered the basics, you can:</p> <ul> <li>Explore the MoE modules.</li> <li>Check out the VLA components.</li> <li>Read our Contributing Guide to add your own blocks!</li> </ul> <p>Happy building! \ud83e\uddf1\u2728</p>"},{"location":"api/blocks/","title":"Blocks API","text":""},{"location":"api/blocks/#mlp-blocks","title":"MLP Blocks","text":""},{"location":"api/blocks/#haloblocks.blocks.mlp.mlp","title":"<code>haloblocks.blocks.mlp.mlp</code>","text":""},{"location":"api/blocks/#haloblocks.blocks.mlp.mlp.MLP","title":"<code>MLP</code>","text":"<p>               Bases: <code>Block</code></p> <p>A configurable Multi-Layer Perceptron (MLP) block.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Dimensionality of the input.</p> required <code>hidden_dims</code> <code>List[int]</code> <p>List of hidden layer dimensions.</p> required <code>output_dim</code> <code>Optional[int]</code> <p>Dimensionality of the output.  If None, the last value in <code>hidden_dims</code> is used as output dimension.</p> <code>None</code> <code>activation</code> <code>str</code> <p>Activation function to use between layers.  Supported: 'relu', 'gelu', 'tanh', 'silu'. Defaults to 'relu'.</p> <code>'relu'</code> <code>bias</code> <code>bool</code> <p>Whether to include bias in linear layers. Defaults to True.</p> <code>True</code> <code>last_layer_activation</code> <code>bool</code> <p>If True, applies activation to the final layer. Defaults to False (final layer is strictly linear).</p> <code>False</code> <code>dropout</code> <code>float</code> <p>Dropout probability applied after each activation.</p> <code>0.0</code> Source code in <code>src/haloblocks/blocks/mlp/mlp.py</code> <pre><code>@BlockRegistry.register(\"mlp\")\nclass MLP(Block):\n    \"\"\"\n    A configurable Multi-Layer Perceptron (MLP) block.\n\n    Args:\n        input_dim (int): Dimensionality of the input.\n        hidden_dims (List[int]): List of hidden layer dimensions.\n        output_dim (Optional[int]): Dimensionality of the output. \n            If None, the last value in `hidden_dims` is used as output dimension.\n        activation (str): Activation function to use between layers. \n            Supported: 'relu', 'gelu', 'tanh', 'silu'. Defaults to 'relu'.\n        bias (bool): Whether to include bias in linear layers. Defaults to True.\n        last_layer_activation (bool): If True, applies activation to the final layer.\n            Defaults to False (final layer is strictly linear).\n        dropout (float): Dropout probability applied after each activation.\n    \"\"\"\n\n    _activations = {\n        'relu': nn.ReLU,\n        'gelu': nn.GELU,\n        'tanh': nn.Tanh,\n        'silu': nn.SiLU\n    }\n\n    def __init__(\n        self, \n        input_dim: int, \n        hidden_dims: List[int], \n        output_dim: Optional[int] = None,\n        activation: str = 'relu',\n        bias: bool = True,\n        last_layer_activation: bool = False,\n        dropout: float = 0.0\n    ):\n        super().__init__()\n\n        self.input_dim = input_dim\n        self.hidden_dims = hidden_dims\n        self.output_dim = output_dim or hidden_dims[-1]\n        self.activation_name = activation.lower()\n        self.use_bias = bias\n        self.last_layer_activation = last_layer_activation\n\n        if self.activation_name not in self._activations:\n            raise ValueError(f\"Unsupported activation: {activation}. Supported: {list(self._activations.keys())}\")\n\n        activation_cls = self._activations[self.activation_name]\n\n        layers = []\n        curr_dim = input_dim\n\n        # Build hidden layers\n        for i, h_dim in enumerate(hidden_dims):\n            # If this is the last hidden layer and no explicit output_dim, check if we apply activation\n            is_last = (i == len(hidden_dims) - 1) and (output_dim is None)\n\n            layers.append(nn.Linear(curr_dim, h_dim, bias=bias))\n\n            if not is_last or last_layer_activation:\n                layers.append(activation_cls())\n                if dropout &gt; 0:\n                    layers.append(nn.Dropout(dropout))\n\n            curr_dim = h_dim\n\n        # Add output layer if explictly defined\n        if output_dim is not None:\n            layers.append(nn.Linear(curr_dim, output_dim, bias=bias))\n            if last_layer_activation:\n                layers.append(activation_cls())\n                if dropout &gt; 0:\n                    layers.append(nn.Dropout(dropout))\n\n        self.model = nn.Sequential(*layers)\n\n    def forward(self, x: torch.Tensor, **kwargs) -&gt; torch.Tensor:\n        \"\"\"\n        Forward pass of the MLP.\n\n        Args:\n            x (torch.Tensor): Input tensor of shape [..., input_dim].\n            **kwargs: Ignored.\n\n        Returns:\n            torch.Tensor: Output tensor of shape [..., output_dim].\n        \"\"\"\n        return self.model(x)\n\n    def __repr__(self):\n        return (f\"MLP(input_dim={self.input_dim}, hidden_dims={self.hidden_dims}, \"\n                f\"output_dim={self.output_dim}, activation={self.activation_name}, \"\n                f\"bias={self.use_bias}, last_activation={self.last_layer_activation})\")\n</code></pre>"},{"location":"api/blocks/#haloblocks.blocks.mlp.mlp.MLP.forward","title":"<code>forward(x, **kwargs)</code>","text":"<p>Forward pass of the MLP.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape [..., input_dim].</p> required <code>**kwargs</code> <p>Ignored.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>torch.Tensor: Output tensor of shape [..., output_dim].</p> Source code in <code>src/haloblocks/blocks/mlp/mlp.py</code> <pre><code>def forward(self, x: torch.Tensor, **kwargs) -&gt; torch.Tensor:\n    \"\"\"\n    Forward pass of the MLP.\n\n    Args:\n        x (torch.Tensor): Input tensor of shape [..., input_dim].\n        **kwargs: Ignored.\n\n    Returns:\n        torch.Tensor: Output tensor of shape [..., output_dim].\n    \"\"\"\n    return self.model(x)\n</code></pre>"},{"location":"api/blocks/#attention-blocks","title":"Attention Blocks","text":""},{"location":"api/blocks/#haloblocks.blocks.attention.self_attention","title":"<code>haloblocks.blocks.attention.self_attention</code>","text":""},{"location":"api/blocks/#haloblocks.blocks.attention.self_attention.HeadAttn","title":"<code>HeadAttn</code>","text":"<p>               Bases: <code>Block</code></p> <p>A single attention head often used as a component of Multi-Head Attention.</p> <p>This block projects the input to a smaller <code>head_size</code> and computes self-attention within that subspace.</p> <p>Parameters:</p> Name Type Description Default <code>emb_dim</code> <code>int</code> <p>Dimensionality of the input embeddings.</p> <code>256</code> <code>head_size</code> <code>int</code> <p>Dimensionality of the head (typically emb_dim // num_heads).</p> <code>16</code> <code>drop_fact</code> <code>float</code> <p>Dropout probability. Defaults to 0.0.</p> <code>0.0</code> <code>causal_mask</code> <code>bool</code> <p>If True, applies a causal mask for auto-regressive decoding.</p> <code>False</code> <code>return_attn_weights</code> <code>bool</code> <p>If True, returns both the output and the attention weights.</p> <code>False</code> Source code in <code>src/haloblocks/blocks/attention/self_attention.py</code> <pre><code>@BlockRegistry.register(\"head_attention\")\nclass HeadAttn(Block):\n    \"\"\"\n    A single attention head often used as a component of Multi-Head Attention.\n\n    This block projects the input to a smaller `head_size` and computes\n    self-attention within that subspace.\n\n    Args:\n        emb_dim (int): Dimensionality of the input embeddings.\n        head_size (int): Dimensionality of the head (typically emb_dim // num_heads).\n        drop_fact (float): Dropout probability. Defaults to 0.0.\n        causal_mask (bool): If True, applies a causal mask for auto-regressive decoding.\n        return_attn_weights (bool): If True, returns both the output and the attention weights.\n    \"\"\"\n    def __init__(self, emb_dim=256, head_size=16, drop_fact=0.0, causal_mask=False, return_attn_weights=False):\n        super().__init__()\n        self.emb_dim = emb_dim\n        self.head_size = head_size\n        self.causal_mask = causal_mask\n        self.return_attn_weights = return_attn_weights\n\n        self.query_proj = nn.Linear(emb_dim, head_size, bias=False)\n        self.key_proj = nn.Linear(emb_dim, head_size, bias=False)\n        self.val_proj = nn.Linear(emb_dim, head_size, bias=False)\n\n        self.scale = 1.0 / math.sqrt(head_size)\n        self.dropout = nn.Dropout(drop_fact)\n\n    def forward(self, inputs, **kwargs):\n        B, seq_len, D = inputs.shape\n        Query = self.query_proj(inputs)\n        Key = self.key_proj(inputs)\n        Val = self.val_proj(inputs)\n\n        scores = torch.matmul(Query, Key.transpose(-2, -1)) * self.scale\n\n        if self.causal_mask:\n            causal_tril = torch.tril(torch.ones(seq_len, seq_len, dtype=torch.bool, device=inputs.device))\n            scores = scores.masked_fill(causal_tril == 0, float('-inf'))\n\n        attn_weights = F.softmax(scores, dim=-1)\n        attn_weights = self.dropout(attn_weights)\n        attn_output = torch.matmul(attn_weights, Val)\n\n        if self.return_attn_weights:\n            return attn_output, attn_weights\n        else:\n            return attn_output \n</code></pre>"},{"location":"api/blocks/#haloblocks.blocks.attention.self_attention.MultiHeadAttn","title":"<code>MultiHeadAttn</code>","text":"<p>               Bases: <code>Block</code></p> <p>Multi-Head Attention (MHA) block.</p> <p>MHA allows the model to jointly attend to information from different representation subspaces at different positions. This implementation uses a collection of <code>HeadAttn</code> blocks.</p> <p>Parameters:</p> Name Type Description Default <code>emb_dim</code> <code>int</code> <p>Dimensionality of the input embeddings.</p> <code>256</code> <code>num_heads</code> <code>int</code> <p>Number of attention heads.</p> <code>8</code> <code>drop_fact</code> <code>float</code> <p>Dropout probability. Defaults to 0.0.</p> <code>0.0</code> <code>causal_mask</code> <code>bool</code> <p>If True, applies causal masking to all heads.</p> <code>False</code> <code>return_attn_weights</code> <code>bool</code> <p>If True, returns both the output and the attention weights.</p> <code>False</code> Source code in <code>src/haloblocks/blocks/attention/self_attention.py</code> <pre><code>@BlockRegistry.register(\"multi_head_attn\")\nclass MultiHeadAttn(Block):\n    \"\"\"\n    Multi-Head Attention (MHA) block.\n\n    MHA allows the model to jointly attend to information from different\n    representation subspaces at different positions. This implementation\n    uses a collection of `HeadAttn` blocks.\n\n    Args:\n        emb_dim (int): Dimensionality of the input embeddings.\n        num_heads (int): Number of attention heads.\n        drop_fact (float): Dropout probability. Defaults to 0.0.\n        causal_mask (bool): If True, applies causal masking to all heads.\n        return_attn_weights (bool): If True, returns both the output and the attention weights.\n    \"\"\"\n    def __init__(self, emb_dim=256, num_heads=8, drop_fact=0.0, causal_mask=False, return_attn_weights=False):\n        super().__init__()\n        self.emb_dim = emb_dim\n        self.num_heads = num_heads\n\n        if emb_dim % num_heads != 0:\n            raise ValueError(f\"emb_dim ({emb_dim}) must be divisible by num_heads ({num_heads})\")\n\n        self.head_size = emb_dim // num_heads\n        self.heads = nn.ModuleList([\n            HeadAttn(emb_dim, head_size=self.head_size, drop_fact=drop_fact, \n                     causal_mask=causal_mask, return_attn_weights=return_attn_weights) \n            for _ in range(num_heads)\n        ])\n\n        self.proj = nn.Linear(emb_dim, emb_dim)\n        self.dropout = nn.Dropout(drop_fact)\n\n    def forward(self, inputs, **kwargs):\n        # inputs: [B, Seq_len, D]\n        head_outputs = [head(inputs) for head in self.heads]\n\n        # Concatenate head outputs\n        concat_output = torch.cat(head_outputs, dim=-1)\n\n        # Final linear projection\n        output = self.proj(concat_output)\n        output = self.dropout(output)\n\n        return output\n</code></pre>"},{"location":"api/blocks/#haloblocks.blocks.attention.self_attention.SelfAttn","title":"<code>SelfAttn</code>","text":"<p>               Bases: <code>Block</code></p> <p>A basic Single-Head Self-Attention block.</p> <p>This block implements the standard scaled dot-product self-attention mechanism where queries, keys, and values are projected from the same input.</p> <p>Parameters:</p> Name Type Description Default <code>emb_dim</code> <code>int</code> <p>Dimensionality of the input embeddings.</p> required <code>return_attn_weights</code> <code>bool</code> <p>If True, returns both the output and the attention weights. Defaults to False.</p> <code>False</code> Source code in <code>src/haloblocks/blocks/attention/self_attention.py</code> <pre><code>@BlockRegistry.register(\"self_attention_basic\")\nclass SelfAttn(Block):\n    \"\"\"\n    A basic Single-Head Self-Attention block.\n\n    This block implements the standard scaled dot-product self-attention mechanism\n    where queries, keys, and values are projected from the same input.\n\n    Args:\n        emb_dim (int): Dimensionality of the input embeddings.\n        return_attn_weights (bool): If True, returns both the output and the\n            attention weights. Defaults to False.\n    \"\"\"\n    def __init__(self, emb_dim, return_attn_weights=False):\n        super().__init__()\n        self.emb_dim = emb_dim\n        self.return_attn_weights = return_attn_weights\n\n        # Linear projections for Query, Key, Value\n        self.query_proj = nn.Linear(emb_dim, emb_dim, bias=False)\n        self.key_proj = nn.Linear(emb_dim, emb_dim, bias=False)\n        self.val_proj = nn.Linear(emb_dim, emb_dim, bias=False)\n\n        self.scale = 1.0 / math.sqrt(emb_dim)\n\n    def forward(self, inputs, causal_mask=None, **kwargs):\n        # inputs: [B, Seq_len, D]\n        Query = self.query_proj(inputs)\n        Key = self.key_proj(inputs)\n        Val = self.val_proj(inputs)\n\n        # Compute attention scores\n        attn_scores = torch.matmul(Query, Key.transpose(-2, -1)) * self.scale\n\n        if causal_mask is not None:\n            attn_scores = attn_scores.masked_fill(causal_mask == 0, float('-inf'))\n\n        attn_weights = F.softmax(attn_scores, dim=-1)\n\n        # Weighted sum of values\n        attn_output = torch.matmul(attn_weights, Val)\n\n        if self.return_attn_weights:\n            return attn_output, attn_weights\n        else:\n            return attn_output  \n</code></pre>"},{"location":"api/blocks/#haloblocks.blocks.attention.scaledotprod","title":"<code>haloblocks.blocks.attention.scaledotprod</code>","text":""},{"location":"api/blocks/#haloblocks.blocks.attention.scaledotprod.ScaledDotProductAttention","title":"<code>ScaledDotProductAttention</code>","text":"<p>               Bases: <code>Block</code></p> <p>Standard Scaled Dot-Product Attention mechanism.</p> <p>This block computes attention as: Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V</p> <p>Parameters:</p> Name Type Description Default <code>dropout</code> <code>float</code> <p>Dropout probability applied to attention weights. Defaults to 0.1.</p> <code>0.1</code> Source code in <code>src/haloblocks/blocks/attention/scaledotprod.py</code> <pre><code>@BlockRegistry.register(\"scaled_dot_product_attention\")\nclass ScaledDotProductAttention(Block):\n    \"\"\"\n    Standard Scaled Dot-Product Attention mechanism.\n\n    This block computes attention as:\n    Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V\n\n    Args:\n        dropout (float): Dropout probability applied to attention weights.\n            Defaults to 0.1.\n    \"\"\"\n    def __init__(self, dropout=0.1):\n        super().__init__()\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, q, k, v, mask=None, **kwargs):\n        \"\"\"\n        Computes the attention output.\n\n        Args:\n            q (torch.Tensor): Query tensor.\n            k (torch.Tensor): Key tensor.\n            v (torch.Tensor): Value tensor.\n            mask (torch.Tensor, optional): Mask to apply to attention scores.\n            **kwargs: Ignored.\n\n        Returns:\n            torch.Tensor: The attention output.\n        \"\"\"\n        scores = torch.matmul(q, k.transpose(-2, -1)) / math.sqrt(q.size(-1))\n        if mask is not None:\n            scores = scores.masked_fill(mask == 0, -1e9)\n        attn = self.dropout(F.softmax(scores, dim=-1))\n        return torch.matmul(attn, v)\n</code></pre>"},{"location":"api/blocks/#haloblocks.blocks.attention.scaledotprod.ScaledDotProductAttention.forward","title":"<code>forward(q, k, v, mask=None, **kwargs)</code>","text":"<p>Computes the attention output.</p> <p>Parameters:</p> Name Type Description Default <code>q</code> <code>Tensor</code> <p>Query tensor.</p> required <code>k</code> <code>Tensor</code> <p>Key tensor.</p> required <code>v</code> <code>Tensor</code> <p>Value tensor.</p> required <code>mask</code> <code>Tensor</code> <p>Mask to apply to attention scores.</p> <code>None</code> <code>**kwargs</code> <p>Ignored.</p> <code>{}</code> <p>Returns:</p> Type Description <p>torch.Tensor: The attention output.</p> Source code in <code>src/haloblocks/blocks/attention/scaledotprod.py</code> <pre><code>def forward(self, q, k, v, mask=None, **kwargs):\n    \"\"\"\n    Computes the attention output.\n\n    Args:\n        q (torch.Tensor): Query tensor.\n        k (torch.Tensor): Key tensor.\n        v (torch.Tensor): Value tensor.\n        mask (torch.Tensor, optional): Mask to apply to attention scores.\n        **kwargs: Ignored.\n\n    Returns:\n        torch.Tensor: The attention output.\n    \"\"\"\n    scores = torch.matmul(q, k.transpose(-2, -1)) / math.sqrt(q.size(-1))\n    if mask is not None:\n        scores = scores.masked_fill(mask == 0, -1e9)\n    attn = self.dropout(F.softmax(scores, dim=-1))\n    return torch.matmul(attn, v)\n</code></pre>"},{"location":"api/core/","title":"Core API","text":""},{"location":"api/core/#core-api_1","title":"Core API","text":""},{"location":"api/core/#haloblocks.core.factory","title":"<code>haloblocks.core.factory</code>","text":""},{"location":"api/core/#haloblocks.core.factory.BlockFactory","title":"<code>BlockFactory</code>","text":"<p>A factory for creating Block instances from configuration dictionaries.</p> <p>The factory supports recursive building of nested blocks (e.g., CompositeBlocks) by identifying \"blocks\" keys in the configuration.</p> Source code in <code>src/haloblocks/core/factory.py</code> <pre><code>class BlockFactory:\n    \"\"\"\n    A factory for creating Block instances from configuration dictionaries.\n\n    The factory supports recursive building of nested blocks (e.g., CompositeBlocks)\n    by identifying \"blocks\" keys in the configuration.\n    \"\"\"\n    @staticmethod\n    def create(config_or_type: str | dict, **kwargs) -&gt; Block:\n        \"\"\"\n        Instantiates a Block based on the provided configuration.\n\n        Supports two invocation styles:\n        1. Dictionary: create({'type': 'mha', 'dim': 128})\n        2. Keyword: create('mha', dim=128)\n\n        Args:\n            config_or_type (str | dict): Either a configuration dictionary\n                containing \"type\" or the string name of the block type.\n            **kwargs: Parameters passed to the block constructor if config_or_type is a string.\n\n        Returns:\n            Block: An initialized Block instance.\n        \"\"\"\n        if isinstance(config_or_type, dict):\n            config = config_or_type.copy()\n            block_type = config.pop(\"type\")\n            params = config\n        else:\n            block_type = config_or_type\n            params = kwargs\n\n        block_cls = BlockRegistry.get(block_type)\n\n        # Recursively build nested blocks if needed\n        if \"blocks\" in params:\n            sub_blocks = []\n            for sub in params[\"blocks\"]:\n                if isinstance(sub, dict):\n                    sub_blocks.append(BlockFactory.create(sub))\n                elif isinstance(sub, (list, tuple)) and len(sub) == 2 and isinstance(sub[1], dict):\n                    sub_blocks.append(BlockFactory.create(sub[0], **sub[1]))\n                else:\n                    sub_blocks.append(sub)\n            params[\"blocks\"] = sub_blocks\n\n        return block_cls(**params)\n</code></pre>"},{"location":"api/core/#haloblocks.core.factory.BlockFactory.create","title":"<code>create(config_or_type, **kwargs)</code>  <code>staticmethod</code>","text":"<p>Instantiates a Block based on the provided configuration.</p> <p>Supports two invocation styles: 1. Dictionary: create({'type': 'mha', 'dim': 128}) 2. Keyword: create('mha', dim=128)</p> <p>Parameters:</p> Name Type Description Default <code>config_or_type</code> <code>str | dict</code> <p>Either a configuration dictionary containing \"type\" or the string name of the block type.</p> required <code>**kwargs</code> <p>Parameters passed to the block constructor if config_or_type is a string.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>Block</code> <code>Block</code> <p>An initialized Block instance.</p> Source code in <code>src/haloblocks/core/factory.py</code> <pre><code>@staticmethod\ndef create(config_or_type: str | dict, **kwargs) -&gt; Block:\n    \"\"\"\n    Instantiates a Block based on the provided configuration.\n\n    Supports two invocation styles:\n    1. Dictionary: create({'type': 'mha', 'dim': 128})\n    2. Keyword: create('mha', dim=128)\n\n    Args:\n        config_or_type (str | dict): Either a configuration dictionary\n            containing \"type\" or the string name of the block type.\n        **kwargs: Parameters passed to the block constructor if config_or_type is a string.\n\n    Returns:\n        Block: An initialized Block instance.\n    \"\"\"\n    if isinstance(config_or_type, dict):\n        config = config_or_type.copy()\n        block_type = config.pop(\"type\")\n        params = config\n    else:\n        block_type = config_or_type\n        params = kwargs\n\n    block_cls = BlockRegistry.get(block_type)\n\n    # Recursively build nested blocks if needed\n    if \"blocks\" in params:\n        sub_blocks = []\n        for sub in params[\"blocks\"]:\n            if isinstance(sub, dict):\n                sub_blocks.append(BlockFactory.create(sub))\n            elif isinstance(sub, (list, tuple)) and len(sub) == 2 and isinstance(sub[1], dict):\n                sub_blocks.append(BlockFactory.create(sub[0], **sub[1]))\n            else:\n                sub_blocks.append(sub)\n        params[\"blocks\"] = sub_blocks\n\n    return block_cls(**params)\n</code></pre>"},{"location":"api/core/#haloblocks.core.registry","title":"<code>haloblocks.core.registry</code>","text":""},{"location":"api/core/#haloblocks.core.registry.BlockRegistry","title":"<code>BlockRegistry</code>","text":"<p>A central registry for mapping string identifiers to Block classes.</p> <p>This registry enables configuration-driven model building by allowing the BlockFactory to look up components by their registered names.</p> Source code in <code>src/haloblocks/core/registry.py</code> <pre><code>class BlockRegistry:\n    \"\"\"\n    A central registry for mapping string identifiers to Block classes.\n\n    This registry enables configuration-driven model building by allowing\n    the BlockFactory to look up components by their registered names.\n    \"\"\"\n    _registry = {}\n\n    @classmethod\n    def register(cls, name=None):\n        \"\"\"\n        Decorator to register a Block class.\n\n        Args:\n            name (str, optional): The name to register the block under.\n                Defaults to the class name if not provided.\n        \"\"\"\n        def decorator(block_cls):\n            key = name or block_cls.__name__\n            cls._registry[key] = block_cls\n            return block_cls\n        return decorator\n\n    @classmethod\n    def get(cls, name):\n        \"\"\"\n        Retrieves a registered Block class by name.\n\n        Args:\n            name (str): The registered name of the block.\n\n        Returns:\n            Type[Block]: The registered Block class.\n\n        Raises:\n            KeyError: If the name is not registered.\n        \"\"\"\n        return cls._registry[name]\n</code></pre>"},{"location":"api/core/#haloblocks.core.registry.BlockRegistry.get","title":"<code>get(name)</code>  <code>classmethod</code>","text":"<p>Retrieves a registered Block class by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The registered name of the block.</p> required <p>Returns:</p> Type Description <p>Type[Block]: The registered Block class.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the name is not registered.</p> Source code in <code>src/haloblocks/core/registry.py</code> <pre><code>@classmethod\ndef get(cls, name):\n    \"\"\"\n    Retrieves a registered Block class by name.\n\n    Args:\n        name (str): The registered name of the block.\n\n    Returns:\n        Type[Block]: The registered Block class.\n\n    Raises:\n        KeyError: If the name is not registered.\n    \"\"\"\n    return cls._registry[name]\n</code></pre>"},{"location":"api/core/#haloblocks.core.registry.BlockRegistry.register","title":"<code>register(name=None)</code>  <code>classmethod</code>","text":"<p>Decorator to register a Block class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name to register the block under. Defaults to the class name if not provided.</p> <code>None</code> Source code in <code>src/haloblocks/core/registry.py</code> <pre><code>@classmethod\ndef register(cls, name=None):\n    \"\"\"\n    Decorator to register a Block class.\n\n    Args:\n        name (str, optional): The name to register the block under.\n            Defaults to the class name if not provided.\n    \"\"\"\n    def decorator(block_cls):\n        key = name or block_cls.__name__\n        cls._registry[key] = block_cls\n        return block_cls\n    return decorator\n</code></pre>"},{"location":"api/layers/","title":"Layers API","text":""},{"location":"api/layers/#haloblocks.layers","title":"<code>haloblocks.layers</code>","text":""},{"location":"api/layers/#haloblocks.layers.LayersProxy","title":"<code>LayersProxy</code>","text":"<p>A dynamic proxy that provides access to registered Block classes as if they were attributes of this module.</p> Source code in <code>src/haloblocks/layers.py</code> <pre><code>class LayersProxy:\n    \"\"\"\n    A dynamic proxy that provides access to registered Block classes\n    as if they were attributes of this module.\n    \"\"\"\n    def __getattr__(self, name):\n        try:\n            return BlockRegistry.get(name)\n        except KeyError:\n            raise AttributeError(f\"No block registered with name: {name}\")\n\n    def __dir__(self):\n        return list(BlockRegistry._registry.keys())\n</code></pre>"}]}